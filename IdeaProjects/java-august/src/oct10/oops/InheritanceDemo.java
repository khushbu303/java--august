package oct10.oops;

public class InheritanceDemo {
    //inherit properties from existing class from parent to child class
    // IS - A relationship for this we can (extends) <-- keyword
    // super();  avialable to constructor by default
    //this() has to be the first statment in the constructor call
    // super() ha to be the first statmnt in cons. call
}
